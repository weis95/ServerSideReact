import React from 'react';

const Home = () => {
    return (
    <div>
        <div> I am Home </div>
        <button onClick={() => console.log('hi there')}> Press Me </button>
    </div>
    );
};

export default Home;